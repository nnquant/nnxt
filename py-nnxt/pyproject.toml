[build-system]
requires = ["maturin>=1.5"]
build-backend = "maturin"

[project]
name = "nnxt"
version = "0.1.0"
description = "nnxt python bindings"
requires-python = ">=3.9"

[tool.maturin]
module-name = "nnxt"
develop-args = ["--editable"]
